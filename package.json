{
  "name": "theonepagebible",
  "description": "A simple web app that displays the entire Bible on one page.",
  "version": "1.0.0",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server/server.js",
    "format": "npx prettier --write **/*.{js,md}",
    "format:check": "npx prettier --check **/*.{js,md}",
    "lint": "npx eslint server/*.js --env=node && npx eslint client/*.js --env=browser"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "dotenv": "^16.3.1",
    "ejs": "^3.1.6",
    "express": "^4.17.3",
    "firebase": "^10.5.0",
    "node-fetch": "^3.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
